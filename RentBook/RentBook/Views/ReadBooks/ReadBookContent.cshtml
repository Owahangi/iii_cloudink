@{
    ViewBag.Title = "ReadBookContent";
}

<h2>ReadBookContent</h2>

@foreach (string c in ViewBag.BookContent)
{
    if(c != "")
    {
        <p>
            @c
        </p>
    }
    //else
    @*{
        <br />
    }*@
}

